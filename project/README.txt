To test the basic approach

Run the sript named: basicBGseparation.
On top of the script, there is a section for user input where user should specify input video and number of object to keep.

To test the panorama approach

Run the sript named: PanoramicSynopsis.
On top of the script, there is a section for user input where user should specify input video and number of object to keep.


RESULTS
All results are put inside the folder called output.
Inside 'output', there are two sub-folders where keeps the result from each implementation.
